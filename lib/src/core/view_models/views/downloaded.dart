import 'package:flutter/foundation.dart';

class DownloadedPageModel extends ChangeNotifier {
  // final ComicDao ComicDao;

  // DownloadedPageModel({this.ComicDao});

  // bool _busy = false;
  // bool get busy => _busy;

  // void setBusy(bool value) {
  //   _busy = value;
  //   notifyListeners();
  // }
  // List<ComicCover> get downloadedComics => ComicDao.downloadedComics;

  // void deleteComic(int index) {
  //   _downloadedComics.removeAt(index);
  //   notifyListeners();
  // }

  // void updateComic(int index) {
  //   // _downloadedComic.removeAt(index);
  //   // notifyListeners();
  // }
}
